# Инструкция по применению миграций базы данных

## 📋 Обзор

Эта инструкция описывает, как применить миграции для создания таблицы пользователей в вашей базе данных Supabase.

## 🗂️ Структура миграций

```
supabase/migrations/
├── 001_create_users_table.sql       # Создание таблицы users
└── 002_create_storage_bucket.sql    # Создание хранилища для иконок
```

## 🚀 Способы применения миграций

### Способ 1: Через SQL Editor в Supabase Dashboard (Рекомендуется)

1. Откройте ваш проект в [Supabase Dashboard](https://app.supabase.com)

2. Перейдите в раздел **SQL Editor** (слева в меню)

3. Нажмите **New Query**

4. Скопируйте содержимое файла `supabase/migrations/001_create_users_table.sql`

5. Вставьте в редактор и нажмите **Run** (или `Ctrl+Enter`)

6. Повторите шаги 3-5 для файла `002_create_storage_bucket.sql`

7. Проверьте результат:
   - Перейдите в **Table Editor** - должна появиться таблица `users`
   - Перейдите в **Storage** - должен появиться bucket `user-icons`

### Способ 2: Через Supabase CLI (Для продвинутых)

Если у вас установлен Supabase CLI:

```bash
# Инициализация Supabase проекта (если еще не сделано)
supabase init

# Связать с вашим проектом
supabase link --project-ref ВАШ_PROJECT_ID

# Применить миграции
supabase db push
```

### Способ 3: Ручное копирование SQL

1. Откройте файл `supabase/migrations/001_create_users_table.sql`

2. Скопируйте весь SQL код

3. Перейдите в Supabase Dashboard → SQL Editor

4. Вставьте код и выполните

5. Повторите для `002_create_storage_bucket.sql`

## 📊 Что создается

### Таблица `users`

| Колонка      | Тип                  | Описание                           |
|--------------|----------------------|------------------------------------|
| id           | UUID (PRIMARY KEY)   | ID пользователя (из auth.users)    |
| nickname     | VARCHAR(50) UNIQUE   | Никнейм (3-50 символов)            |
| email        | VARCHAR(255) UNIQUE  | Электронная почта                  |
| icon         | TEXT                 | URL иконки/аватара                 |
| birth_date   | DATE                 | Дата рождения                      |
| created_at   | TIMESTAMP            | Дата создания профиля              |

### Storage Bucket `user-icons`

- **Название**: `user-icons`
- **Публичный доступ**: Да
- **Структура**: `user-icons/{user_id}/filename.ext`

## 🔒 Настроенная безопасность

### Row Level Security (RLS)

✅ Все пользователи могут **просматривать** профили других пользователей
✅ Пользователи могут **создавать** только свой профиль
✅ Пользователи могут **изменять** только свой профиль
✅ Пользователи могут **удалять** только свой профиль

### Storage Policies

✅ Все могут **просматривать** иконки
✅ Пользователи могут **загружать** иконки только в свою папку
✅ Пользователи могут **изменять/удалять** только свои иконки

## 🔄 Автоматическое создание профиля

При регистрации нового пользователя:
- Автоматически создается запись в таблице `users`
- Никнейм берется из metadata регистрации или генерируется из email
- Email копируется из auth.users

## 💻 Использование в коде Flutter

### Регистрация пользователя

```dart
import 'package:cruch/services/supabase_service.dart';

// Регистрация с никнеймом
final response = await SupabaseService.signUpWithEmail(
  email: 'user@example.com',
  password: 'securePassword123',
  data: {
    'nickname': 'cooluser',
  },
);

// Профиль создастся автоматически!
```

### Работа с профилем

```dart
import 'package:cruch/repositories/user_repository.dart';

// Получить текущего пользователя
final user = await UserRepository.getCurrentUser();
print('Привет, ${user?.nickname}!');

// Получить пользователя по ID
final user = await UserRepository.getUserById('user-id');

// Обновить никнейм
await UserRepository.updateNickname(userId, 'newNickname');

// Обновить иконку
await UserRepository.updateIcon(userId, 'https://example.com/icon.png');

// Обновить дату рождения
await UserRepository.updateBirthDate(userId, DateTime(1990, 1, 1));

// Поиск пользователей
final users = await UserRepository.searchByNickname('cool');
```

### Загрузка иконки

```dart
import 'dart:typed_data';
import 'package:cruch/services/supabase_service.dart';

Future<String> uploadUserIcon(Uint8List imageBytes, String userId) async {
  final fileName = 'avatar_${DateTime.now().millisecondsSinceEpoch}.jpg';
  final path = '$userId/$fileName';
  
  // Загрузить файл
  final url = await SupabaseService.uploadFile(
    'user-icons',
    path,
    imageBytes,
  );
  
  // Обновить профиль
  await UserRepository.updateIcon(userId, url);
  
  return url;
}
```

## ✅ Проверка установки

После применения миграций проверьте:

1. **Таблица создана**:
   - Dashboard → Table Editor → Должна быть таблица `users`

2. **RLS включен**:
   - Откройте таблицу → вкладка "Policies"
   - Должно быть 4 политики

3. **Storage bucket создан**:
   - Dashboard → Storage → Должен быть bucket `user-icons`

4. **Триггер работает**:
   ```sql
   -- Выполните в SQL Editor
   SELECT * FROM pg_trigger WHERE tgname = 'on_auth_user_created';
   ```
   Должна вернуться одна строка

## 🐛 Решение проблем

### Ошибка: "relation already exists"

Таблица уже существует. Либо удалите её:
```sql
DROP TABLE IF EXISTS public.users CASCADE;
```
И выполните миграцию снова, либо пропустите этот шаг.

### Ошибка: "permission denied"

Убедитесь, что вы вошли как владелец проекта в Supabase Dashboard.

### Триггер не работает

Проверьте, что функция создана:
```sql
SELECT * FROM pg_proc WHERE proname = 'handle_new_user';
```

## 📝 Откат миграций (если нужно)

Если нужно откатить изменения:

```sql
-- Удалить триггер
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;

-- Удалить функцию
DROP FUNCTION IF EXISTS public.handle_new_user();

-- Удалить таблицу
DROP TABLE IF EXISTS public.users CASCADE;

-- Удалить storage bucket (через Dashboard → Storage → Settings)
```

## 🎉 Готово!

После применения миграций ваша база данных готова к работе!

Теперь вы можете:
- ✅ Регистрировать пользователей
- ✅ Работать с профилями через `UserRepository`
- ✅ Загружать иконки пользователей
- ✅ Искать пользователей по никнейму

